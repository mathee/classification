# columns to be loaded from original csv, for training and testing
SELECTED_COLUMNS = ["HasDetections",
                    "AVProductStatesIdentifier",
                    "AVProductsInstalled"
                    ]