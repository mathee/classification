"""PLACEHOLDER FILE FOR DATA WRANGLING:
Script would contain steps/functions that are required in order to gather/mine data
from different sources, compose it into a single file/table combining all raw data 
in one place. Then save it somewhere (disc, database)"""

'''
import requests # in case of scraping
import re # regular expressions for extracting stuff from scraped content
import psychopg # in case data is on PostgreSQL
import pymongo # in case data is on MongoDB
from config import PATH_XTRAIN, PATH_YTRAIN, PATH_XTEST, PATH_YTEST


def wrangle_trainingdata():
    return None

def wrangle_testdata():
    return None
'''